@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model List<RoleViewModel>

    <p>
    @Html.ActionLink("انشاء دور جديد", "Create")
    </p>

<table class="table table-striped table-bordered table-hover">
        <tr>
            <th>
                الاسم
            </th>
            <th></th>
        </tr>
    @foreach(var item in Model)
    {
      <tr>
          <td>@item.Name</td>
          <td>
            <td>
                <a asp-controller="Roles" asp-action="Edit" asp-route-id="@item.Id"> تعديل </a> |
                <a asp-controller="Roles" asp-action="Details" asp-route-id="@item.Id"> تفاصيل </a> |
                <a asp-controller="Roles" asp-action="Delete" asp-route-id="@item.Id"> حذف </a>
            </td>
          </td>
      </tr>  
    }
</table>
